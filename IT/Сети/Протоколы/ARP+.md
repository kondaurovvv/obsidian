**ARP** (Address Resolution Protocol или протокол разрешения адресов) — это ==сетевой протокол, который нужен для того, чтобы узнать физический **MAC-адрес** устройства в локальной сети по его известному логическому **IP-адресу**==.

- Альтернативное название RFC: Converting Network Protocol Addresses to 48.bit Ethernet Address for Transmission on Ethernet Hardware;
- Может работать с любым протоколом, использующим MAC-адреса и поддерживающим широковещательную рассылку.

#### Принцип работы ARP
- В какой ситуации необходим ARP:
    - "Отправителю" требуется отправить узлу, находящемуся с ним в одной среде Ethernet ("получателю"), IP-пакет;
    - IP-адрес получателя известен;
    - Mac-адрес получателя неизвестен.
- ARP запрашивает MAC получателя, указывая его IP:
    - Запрос идет в широковещательном кадре;
    - "Получатель" видит свой IP-адрес в запросе и отпраляет в ответ кадр со своими IP и MAC-адресом;
    - "Отправитель", зная MAC, отправляет оригинальный пакет.

#### Кэш ARP
- Отправлять запрос ARP на каждый пакет накладно;
- IP-адреса и MAC-адреса у узлов меняются редко;
- Разумно в течение некоторого времени хранить известные пары IP+MAC в промежуточной таблице:
    - Пополнять таблицу автоматически при получении любого ARP-пакета (он содержит IP и MAC отправителя);
    - Но не хранить привязки вечно, они должны устаревать;
- Если запись в таблице -пакет отправляется, если нет - откладывается в буфер или удаляется.

#### Формат пакета ARP
- В случае IP+Ethernet:
    - Инкапсуляция: ARPA, Ethertype: 0x0806;
    - HTYPE: 0x0001, PTYPE: 0x0800;
    - HLEN: 0x06, PLEN: 0x04;
    - OPER:
        - 0x0001 для запроса
        - 0x0002 для ответа

![[Структура_ARP_пакета.png|900]]

| Английское название                | Русский перевод                  | Размер bit | Краткое описание                                                                   |
| ---------------------------------- | -------------------------------- | ---------- | ---------------------------------------------------------------------------------- |
| **Hardware Type (HTYPE)**          | **Тип оборудования**             | 16         | Тип сетевого протокола канального уровня. Для Ethernet равен `1`.                  |
| **Protocol Type (PTYPE)**          | **Тип протокола**                | 16         | Тип сетевого протокола, для которого запрашивается адрес. Для IPv4 равен `0x0800`. |
| **Hardware Address Length (HLEN)** | **Длина аппаратного адреса**     | 8          | Размер физического адреса в байтах. Для Ethernet (MAC) равен `6` (48 бит).         |
| **Protocol Address Length (PLEN)** | **Длина адреса протокола**       | 8          | Размер логического адреса в байтах. Для IPv4 равен `4` (32 бита).                  |
| **Operation (OPER)**               | **Код операции**                 | 16         | Задает тип сообщения: `1` — ARP-запрос (Request), `2` — ARP-ответ (Reply).         |
| **Sender Hardware Address (SHA)**  | **Аппаратный адрес отправителя** | 48         | MAC-адрес устройства, которое отправляет данный пакет.                             |
| **Sender Protocol Address (SPA)**  | **Адрес протокола отправителя**  | 32         | IP-адрес устройства, которое отправляет данный пакет.                              |
| **Target Hardware Address (THA)**  | **Целевой аппаратный адрес**     | 48         | MAC-адрес получателя. В запросе не заполнен и состоит из нулей (`00...00`).        |
| **Target Protocol Address (TPA)**  | **Целевой адрес протокола**      | 32         | IP-адрес узла, чей физический MAC-адрес необходимо определить.                     |

![[Структура_APR_WS_пакета.png|900]]

#### Proxy ARP (в идеале избегать использование)
- Если нарушен принцип "одна канальная среда - одна сеть IP", то стандартное поведение ARP не позволит узнать привязку IP к канальному адресу;
- Proxy ARP - костыль для подобных случаев:
    - MAC получателя на самом деле не интересен;
    - Маршрутизатор возвращает свой MAC в ответ.
- Может использоваться вместо default gateway:
    - Но не стоит: вызывает переполнение кэша ARP.

#### Конфликты IP-адресов
- ARP может использоваться перед назначением IP-адреса на интерфейс для проверки его незанятости:
    - Или для поднятия тревоги, если кто-то другой пытается выдать свой MAC для вашего IP.
- ARP Probe (ACD, RFC 5227): OPER=1, SPA=0, TPA=IP:
    - **Sender Protocol Address (SPA) = 0** — это верно (ты ответил правильно). Именно нулевой адрес отправителя (`0.0.0.0`) гарантирует, что соседние узлы **не будут обновлять** свои ARP-таблицы на основе этого запроса, так как узел ещё не подтвердил уникальность адреса.
    - Используется перед инициализацией адреса.
    - ARP Probe используется **исключительно для обнаружения конфликта адресов (DAD)** _до_ того, как узел начнёт использовать адрес.
    - Два режима работы:
        - OPER=1, SPA=TPA=IP, THA=0;
        - OPER=2, SPA=TPA=IP, SHA=THA.
    - Используется после инициализации адреса для обновления кэша остальных клиентов в сети.

#### Смежные с ARP протоколы
- Протоколы, основанные на ARP:
    - Reverse Address Resolution Protocol (RARP) - это ==устаревший сетевой протокол, который использовался бездисковыми компьютерами для автоматического получения своего IP-адреса по известному физическому MAC-адресу с помощью специального сервера==;
    - Inverse Address Resolution Protocol (InARP) — это ==сетевой протокол, который позволяет узнать IP-адрес удаленного устройства по его известному физическому (канальному) адресу==.
- Протоколы, реализующие схожие функции:
    - SLARP (Serial Line Address Resolution Protocol), под протокол Cisco HDLC;
    - NDP (Neighbor Discovery Protocol), под протокол ICMPv6

### Ключевые выводы
- ARP работает только в средах с поддержкой broadcast(широковещательный адрес это всегда **последний адрес в конкретном диапазоне сети**) без него пришлось бы вручную прописывать MAC-адреса на каждом узле;
    
- Broadcast-запросы ARP получают все узлы в сети — это нагрузка, поэтому минимизация broadcast и правильная настройка времени жизни кэша критически важны;
    
- Proxy ARP — вынужденный костыль, при котором маршрутизатор отвечает своим MAC за чужие IP-адреса; его использование приводит к разрастанию ARP-кэша и потенциальным проблемам с памятью;
    
- Gratuitous ARP и ARP Probe позволяют обнаруживать конфликты адресов и обновлять кэши соседей при смене IP или MAC-адреса;

- [[IP+]]
- [[IPv4+]]
- [[IPv6+]]
- [[DHCPv4+]]
- [[ICMPv4+]]
- [[NAT+]]

**СПАСИБО ЗА ОБУЧЕНИЯ [networkeducation](https://www.networkeducation.ru/video/)!**

#it #сети #протоколы #arp